apiVersion: v1
kind: Pod
metadata:
  name: datadisk-jk-pvc
spec:
  containers:
  - image: mongo
    name: mongodb
    volumeMounts:
    - name: datadisk-jk
      mountPath: /T12022/
  volumes:
  - name: datadisk-jk
    persistentVolumeClaim:
      claimName: datadisk-jk
